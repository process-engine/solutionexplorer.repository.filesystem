{
  "name": "@process-engine/solutionexplorer.repository.filesystem",
  "version": "4.3.0",
  "description": "The SolutionExplorer filesystem repository",
  "main": "dist/amd/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && npm run build-commonjs && npm run build-amd",
    "build-commonjs": "tsc",
    "build-amd": "tsc --module amd --outDir ./dist/amd",
    "prepare": "npm run build",
    "lint": "eslint src/*.ts",
    "lint-fix": "eslint --fix src/*.ts",
    "test": ":"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@essential-projects/errors_ts": "^1.5.0",
    "@process-engine/solutionexplorer.contracts": "1.1.0",
    "@process-engine/solutionexplorer.repository.contracts": "feature~add_solution_changed_callback",
    "node-uuid": "^1.4.8"
  },
  "devDependencies": {
    "@essential-projects/eslint-config": "^1.0.0",
    "@process-engine/ci_tools": "^2.0.0",
    "@types/node": "^10.0.0",
    "eslint": "^5.16.0",
    "typescript": "^3.1.6"
  }
}
